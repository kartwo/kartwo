# 二进制产物
/kartwo
/dist-bin/
*.exe

# 数据目录（数据即文件夹，绝不进 git）
/data/

# 本地工具链（go install 到此，不进 git）
/.bin/

# 前端依赖（构建期，不进 git）；构建产物 web/admin/dist 进 git 供 go build embed
/web/admin/ui/node_modules/
/web/admin/ui/.vite/

# 密钥与本地配置（绝不进 git）
*.env
.env
config.enc
*.pem
*.key

# 本地文档：人工测试说明 docs/test、完成报告 docs/reports（不进 git）
/docs/

# 编辑器/系统
.DS_Store
.idea/
.vscode/
*.swp
